# 首页亮点与滚动验收

final result: passed

## 视觉基线与用户调整

- 基线：用户选定的第一张三栏布局，`/Users/kenn/.codex/generated_images/01a0c2ab-3c8a-78b3-a438-55ab33ce005e/exec-a7b261dc-f1a8-4770-b878-fd5801c61a8a.png`。
- 后续明确调整：四段轻度吸附；取消强制满屏、允许下一段露出；页头吸顶，页脚自然收尾；以少量公开项目为主。
- 实现：`http://127.0.0.1:4179/`，仅本地模拟公开数据，无线上业务写入。
- 对照图片为 1397 × 1126；实现使用相同 CSS 视口、deviceScaleFactor 1，无缩放。滚动交互另使用 1440 × 900。
- 原图是连续页面，最终页面按用户后续要求增加分区停靠，因此比较区域内部排版，不将新增停靠间距视为设计偏差。

## 对照证据

截图位于 `/private/tmp/cfk-highlights-qa/`：

- `zh-CN-1397-stage1.png`：首屏整体、精简后的部署卡片、下一段露出。
- `zh-CN-1397-stage2.png`：三栏、细分隔线及视频衔接，文字可完整阅读，无需另裁切。
- `zh-CN-1397-stage3.png`、`zh-CN-1397-stage4.png`：视频、公开加入及页脚。
- `en-1397-stage2.png`：英文三栏与换行。
- `zh-CN-390-stage1.png`、`zh-CN-390-stage2.png`：移动端首屏与纵排亮点。
- `compact-results.json`：最终浏览器断言、布局尺寸与滚动位置。

## 结果与修正

- 字体与文案：使用现有系统字体，保留两行主标题。三栏内容与选图一致并提供英文；免费表述限定于额度内，R2 和付费扩容标为可选。
- 间距与布局：部署卡片恢复短提示与话术；桌面三栏、窄屏纵排。第一轮 941px 附近左右比例失衡，已在 941–1100px 使用等宽列并缩小列间距，复验无溢出。
- 色彩与资产：保留暖白、深橙和原有边框 tokens；沿用真实品牌图片与双语视频，不引入新装饰资产。
- 滚动：用户反馈后，将整屏分区改成有界间距。1440 × 900 下滚轮停靠位置为约 0 / 600 / 1040 / 1543px；页头始终可见。PageUp/PageDown 保留浏览器原生翻页，可到达顶部和底部，不强制每个按键只过一段。
- 交互：中英文切换、部署复制、指南读取和视频播放通过；未登录加入入口复制本地示例话术。拒绝剪贴板时可在首屏就近手动选中复制；减少动态效果和离开首页时关闭吸附。
- 无未解决的 P0/P1/P2 视觉问题。

## 验证范围

- Chromium 测试运行器缺少所需视频编解码能力，改用已安装 Chrome 的隔离无头实例完成验证；无新增浏览器依赖。
- 中英文各覆盖 1397×1126、1440×900、1024×768、941×900、800×1000、760×900、390×844、320×640、1280×480，无横向页面溢出。
- 无页面运行错误或框架错误覆盖层。Mock 未登录请求返回的预期 401 不视为应用错误。
- `npm run typecheck`、现有 Web UI 测试 70/70、`npm run build`、`git diff --check` 通过。
- IAB 已提交打开请求，工具返回 queued；未把 Chrome 的自动化验收表述为 IAB 内验收。
- 本轮不包含真实登录、线上加入、部署、Git 提交或推送；触控板主观手感留给用户在 IAB 体验。
