interface:
  display_name: "cfKanban Deploy"
  short_description: "Deploy and upgrade cfKanban safely on Cloudflare"
  default_prompt: "Use $cfkanban-deploy to produce a read-only capability report and a frozen deployment plan."
policy:
  allow_implicit_invocation: true
